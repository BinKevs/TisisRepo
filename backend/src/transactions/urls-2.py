# from .api import OnlineTransactionViewSet
# from rest_framework.routers import DefaultRouter
# router = DefaultRouter()
# router.register(r'', OnlineTransactionViewSet, basename='online-transactions')

# urlpatterns = router.urls
